import { ResearchClient } from "./ResearchClient";

export const metadata = {
  title: "Research Library · Global CBDC Tracker",
  description: "Curated academic papers, central bank working papers, and policy research on CBDC design, risk, and adoption.",
};

export default function ResearchPage() {
  return <ResearchClient />;
}
